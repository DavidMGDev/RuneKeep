# menu — home screen

**Responsibility:** the app's landing screen — the art-driven main menu that routes to character
selection, creation, and the gallery. Entry: `src/app/index.tsx` → `MenuScreen`.

## Structure
- `menu-screen.tsx` — the single screen.

## Data / deps
Shared UI kit (`AppScreen`, `ChamferBox`, `ArtImage`, `FitLine`, `LoadingScreen`); navigation via
Expo Router.
