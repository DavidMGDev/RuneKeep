# Memory Index

- [v0.34.0 moodboard + the card-text answer](runekeep_v0340_moodboard.md) - the card-text overflow was the DEVICE FONT SCALE all along (measured ~1.15 three ways off one screenshot), so forged cards set allowFontScaling false; REGENERATING catalog.ts re-adds the six H&F ancestry rows v0.12.3 deleted by hand, duplicating every one of them, which is what made the ancestry deck flicker; the trash confused tombstone with splice; the moodboard lives in features/character-sheet/moodboard with pure lib/moodboard + lib/snap - READ before card-text, catalog-generation, trash or moodboard work

- [v0.33.0 + v0.33.1 gotchas](runekeep_v0330_gotchas.md) - the forged-card cache key hashed field LENGTHS so recolouring a card could NEVER invalidate it (now lib/content-sig.ts); the browser image picker returns blob: not data:, which is why a web export lost its portrait; isFilePayload accepted ANY content:// including the app own picked photos; AppScreen dialogs can only dim the inset column without the new overlay slot - ALSO v0.33.1: thumb+source is a LOD pair of TWO files (pointing both at one asset blanks cards on Android); web persistence was NOT coalesced despite the comment claiming it was; history snapshots must not carry inline image bytes (so rewind no longer restores old images); a cosmetic file field must not invalidate the deck memos. READ before card-cache, web image, incoming-file, dialog-dim, carousel-image or web-perf work

- [v0.32.0 cards + engine](runekeep_v0320_cards_engine.md) — domain cards carry REAL names read off the SCANS (the rulebook stores them as flat images, no text layer) so NEVER guess a card id from its level; the engine gained stress/input variables, `overwrite` (a third pass), `floor`, and per-card muting; the void/thevoid split was a HAND EDIT in a generated file — READ before modifier-engine, catalog-generator or card-name work

- [Web stacking + Firefox traps](runekeep_web_stacking_firefox.md) — ONE bug caused "greyed" text fields, washed icons AND offset panels (ChamferBox overlay paints over non-positioned children; needs zIndex -1, zero is not enough); Firefox collapses same-tick history entries and threw players out of the creator; adjustsFontSizeToFit is a no-op on web; RK_FIREFOX=1 drives Firefox — READ before any web rendering or navigation report

- [v0.25.0 pack split + permanent modifiers](runekeep_v0250_split_modifiers.md) — TWO official expansions ('void' = Hope and Fear, 'thevoid' = the beta holding Blood Hunter/Summoner/Blood domain); permanent + chosen CardEffects (Vitality); Elf rest bonus is a modifier now; the CORRECTED card-crop grid; web audio must be WOKEN not created — READ before expansion, modifier or HF card-art work

- [Hope and Fear PDF ground truth](runekeep_hf_pdf_facts.md) — Blood Hunter + Summoner are in NO H&F book (they go to The Void); the H&F ancestry text is ALREADY verbatim-correct; the PDFs carry real text + coordinates so never OCR; ancestry card geometry; scripts/ancestry_marker.py is the committed marking tool — READ before any H&F/Void content, ancestry, or expansion-split work

- [Web + desktop deploy](runekeep_web_deploy.md) — the browser build ALREADY works (`expo export --platform web`, 63MB dist); Cloudflare Pages chosen; docs/web-deploy.md; storage is IndexedDB since v0.24.2 (web-store.ts: sync in-memory map, async persistence, hydrated in _layout) — READ before any web/desktop work

- [Web platform gotchas](runekeep_web_platform_gotchas.md) — the FOUR ways the web build broke (audio API shape differs native vs web, SVGO id collisions, no drag-scroll + image drag ghosts, native-only gate hanging the loading veil) and scripts/web-probe.mjs which drives real Chrome — READ before any web/desktop, SVG, audio or ScrollView work

- [PWA install + stale cache](runekeep_pwa_install.md) — Android needs a SERVICE WORKER (not just a manifest) to install as a real app; install offer leads the welcome tour on mobile web; a reported "Firefox renders wrong" was a CACHED OLD BUILD, fixed by public/_headers — READ before PWA, caching or any "browser X looks wrong" report

- [Tablets = phone frame](runekeep_tablet_phone_frame.md) — v0.24.0 REVERSED the tablet strategy: the tablet runs the PHONE layout inside a magnified 412dp viewport (PhoneFrame at the router root), never per-screen adaptation. Never read window dimensions in a screen; window coords need windowToFrameX/Y; the margins MIRROR the screen (useScreenEdge + useScreenDim), never decorate it. v0.31.0 added the TWO tablet-browser-only traps: a worklet's e.x/e.y is CSS px on web (divide by coordScale, has bitten twice) and the browser keyboard re-magnified the whole app — READ before any layout, gesture-coordinate or overlay-dim work

- [v0.24.0 gotchas](runekeep_v0240_gotchas.md) — Expo Router 404s on shared file URLs (use +native-intent); ONLY the modern expo-file-system reads content:// (legacy throws for WhatsApp/Gmail/Drive); never filter incoming URLs by extension; forged card cache keys on APP VERSION; the Hope and Fear ancestry text was pre-release and wrong — READ before .rkp import, card caching, or trusting bundled card text

- [NO EM DASHES](runekeep_no_em_dashes.md) — owner rule: never an em dash in any user-visible string (labels, placeholders, toasts, errors, release notes). `--` is not a substitute. Plain language over flavour. Code comments exempt. Recorded in AGENTS.md

- [v0.23.0 gotchas](runekeep_v0230_gotchas.md) — build-apk.ps1 never ran prebuild, so EVERY app.json change was silently dropped from the APK (verify the built manifest!); .rkp needs double-escaped pathPattern AND MIME matching for content:// ; RNGH ScrollView required for pan-in-list (bitten twice); tablet breakpoint sw>=600 with phone as a no-op; 3 contextual tours; call them CIRCLE CONTROLS not gears — READ before touching the builder, app.json, list gestures, or tablet layout

- [v0.22.0 state history + DM tokens](runekeep_v0220_state_history.md) — character history is SNAPSHOTS not events (3 reasons replay is impossible); ONE save choke point in redesigned-sheet (never setState in it); card trash is DERIVED from history; schema migration hatch added; DmType/DmGap replaced 21 sizes + 25 gaps; .rkp intent filters + pure routeIncoming (defers mid-edit); onboarding tour — READ before touching character persistence, DM styling, or import

- [No digital dice](runekeep_no_digital_dice.md) — CORE philosophy: the app never rolls dice; only the damage calculator + rest use PHYSICAL results. Never say "attack roll."

- [v0.21.0 spellcast + martial](runekeep_v0210_spellcast_martial.md) — `spellcast` modifier formula variable (computeSheet 4th arg, SUBCLASS_SPELLCAST map, 5 resolution sites) + Mage Robes; Brawler Martial rebuilt to 16 stances/4-per-tier; Classes PDF is PARTIAL (only Assassin/Brawler/Warlock/Witch); ancestry art via by-id VOID_ANCESTRY_ART → ForgedCard fallbackArt (structured cards kept); adversary detail scroll = RNGH ScrollView; void-adversaries.ts now GENERATED (135 blocks) — READ before spellcast/thresholds, formula vars, stances, ancestry art, or the adversary roster

- [v0.15.0 DM Mode](runekeep_v0150_dm_mode.md) — Parties/Sessions/Encounters + roster folders; DmRune desaturated palette + `dm` props (AppScreen/RuneButton/SectionLabel); 3 pure models (party/session/folders) + dm-vitals bridge; global-vs-archive party state (only active encounter + overview write global, completeEncounter freezes archivedGlobal); StatPulse heartbeat (tap=toggle mode, hold=accelerating beats w/ rising-pitch tick, one-log-per-hold); routes are `?id=` query form `as Href` — READ before touching DM screens, party/encounter state, or the pulse control

- [v0.14.0 features (loot, subclass families, experience modifiers)](runekeep_v0140_features.md) — LibraryForgedCard is THE embedded-card renderer (fixes flat weapons); subclass families link by TITLE; `experience` effect target names an INSTANCE via experienceId; loot-data is hand-maintained now (codegen dropped rolls 01-19); StraightCarousel's onIndexChange is a LAGGING MIRROR — commit via centerIndex() — READ before carousel commits, effects targets, loot, or embedded-card rendering

- [Maintainability refactor (2026-06-16)](runekeep_maintainability_refactor.md) — phased PRs #301-304 moved many paths (src/data, redesign→sheet, docs→architecture.md); READ before trusting old paths in the overview
- [Ligma project overview](project_ligma_overview.md) — 5-stage UI mockup pipeline; stack, doc locations, how to orient
- [RuneKeep project overview](project_runekeep_overview.md) — animation-first Daggerheart companion app (Expo SDK 54); stack, repo layout, design oracle
- [RuneKeep dev pipeline](runekeep_dev_pipeline.md) — owner's required PRD→branch→implement→merge flow (no worktrees)
- [Verify animations manually](runekeep_verify_animations_manually.md) — don't screenshot-test motion/loaders; ask owner to check on device
- [Card creation v0.24.3](runekeep_card_creation_v0243.md) — QuickCardFlow (Add Card badge ONLY; Enter-driven, art zone = tap/hold control) + the ONE destination prompt both editors and NFC receive pass through; pure helpers must avoid @/constants/theme or Jest can't load them — READ before card creation or NFC receiving
- [Card types](runekeep_card_types.md) — immutable system cards vs user custom cards (NOTE: v0.3 added in-carousel + gallery editing of custom cards — see [card system v0.3](runekeep_card_system_v03.md))
- [Float menu + interfaces](runekeep_float_menu_roadmap.md) — FPS-wedge radial menu (5 slots, #174) + New Card/Rest/Settings/Level-Up; deck switch moved to gear over-scroll (#174); additive schema fields, pure rest/leveling modules, gitignored rules images
- [APK build + optimization](runekeep_apk_build.md) — apk-build/build-apk.ps1 (arm64, direct-download toolchain, sdkmanager stalls); prebuild first; v1.1.0-android = 2nd release; asset webp opt; offline <90MB, no DB
- [Card-driven modifier system](runekeep_modifier_system.md) — cards carry effects; engine computes the whole sheet; hold-to-toggle + corner check; Modifiers panel replaced Settings (#175, v1.1.0)
- [Card categories overhaul (v0.2)](runekeep_card_categories.md) — N-category looping ring; Druid Wild Shape deck; Notes + Toggle Notes; in-context New Card type chip; name autosize; other-class tracking audit
- [Card tokens (v0.2.5 → v0.6)](runekeep_card_tokens.md) — sewing-button drawer on fullscreen cards; die tokens (d4-d20, dieGeometry); v0.6 = single-outline action button, 2× placed dice (start at 1), float-menu perf gate, multi-page origin preview
- [Sound system (v0.2.11)](runekeep_sound_system.md) — react-native-audio-api SFX engine (src/lib/sfx.ts), risers/pitch-variation/meme-roll, owner's sound mapping, ffmpeg normalize script
- [Card system v0.3](runekeep_card_system_v03.md) — duplicate-card instance ids (catalogIdOf/dedupeIds, respect in future card work); mixed ancestry (cross-out rides slot, ANCESTRY_EFFECT_TRAIT, tunable split regions); custom-card editor; card markdown; SVG/region verification tooling
- [Render perf cost model](runekeep_render_perf.md) — per-card react-native-svg canvases under dim veils tank FPS (the float-menu/scroll lag); plain-View/bake fixes (v0.9.6); NEVER unmount/fade/flicker the carousel behind the float menu (READ before touching card overlays, chamfers, or perf)
- [Library, .rkp & expansions (v0.10.0)](runekeep_library_rkp.md) — app-level Card Library + .rkp file format + homebrew expansions; pure rkp/library modules + library-store + /library screen; what shipped vs DEFERRED (creation-gating/sheet-resolution + NFC); 4 bug fixes (level-up trait reset, domain cards at L3+, inventory button-card, weapons/armor archive)
- [v0.9 / v0.9.1 features](runekeep_v09_features.md) — full multiclass (multiclassSubclassCardId/Domain, half-level pool, mastery lock, hasBeastform gate), Ranger Beastbound companion (companion.ts; v0.9.1 = one lockable card PER FACET via CompanionFacetCard, never-delete), group card move (onReorderCards, LOCKED_CATS), 5-active-domain-card cap, origin hold-to-equip, titleless cards; v0.8 was SKIPPED (folded into v0.9)
- [v0.13.0 features (scars, ancestry flags, typeset)](runekeep_v0130_features.md) — 'scar' effect target rides hope.locked + mixBlendMode saturation wash; featureSectionIndexes is THE feature resolver; FORGE_RENDER_V=17; picker-at-roster via /create?exp=; empty-carousel panel hooks — READ before touching effects, ancestries, or forged-card text
- [v0.13.1 features (Martial Form, NFC catalogId, hope clamp)](runekeep_v0131_features.md) — Martial Form category checklist (the special-category extension points), catalogId NFC share, hope clamp = total−locked at ALL 4 merge sites, FORGE_RENDER_V=18, detached-build rule — READ before special categories, NFC, or hope/scar work
- [The Void expansion (v0.12.5 shipped)](runekeep_void_expansion.md) — v0.12.2 full Void content + gating; v0.12.3 STRUCTURED Void ancestries + modifiers + edit-drag; v0.12.5 found the REAL root causes: forged-card cache (ANY ForgedCard visual change MUST bump FORGE_RENDER_V; async art needs raster:true) + drop-flash killed by the pendingOrderSV id→index bridge (NOT rAF timing); phases C (bespoke mechanics) + D (Recraft banners) remain; READ before any Void or forged-card work
- [v0.13.2 features (sheet NFC receive, catalog sourcing)](runekeep_v0132_features.md) — sheet-side always-on NFC receiving (nfc-gate.ts pure softlock-proof gate + SheetNfcReceiver loop) + drop-from-top landing ceremony (NfcReceiveCeremony, commit-under-overlay flicker-free); gear-browser surfaces ALL custom-expansion record cards (not just ancestries); custom-ancestry passive rides Feature 1 (no author chip); RuneButton auto-shrink; Add-Card badge got the catalog button — READ before NFC, gear-browser sourcing, or custom-ancestry work

- [Forged-cache lag + web profiler](runekeep_forged_cache_perf.md) — the v0.26.1 "laggy everywhere" was the card cache doing a directory listing PER CARD PER PASS and never converging (native only, web measured 60fps); scripts/web-profile.mjs drives a throttled phone viewport and SEED=1 puts a character in IndexedDB so the sheet is one tap away; the PowerShell quote bug that broke two release uploads — READ before any perf report, web measurement, or APK publishing

- [v0.27.0 web cards + Android audio](runekeep_v0270_web_cards_audio.md) — the sheet DROPPED cards with no forged bitmap so the BROWSER lost 5 whole categories (use forgedItem/coverOf); Android RELEASE audio needs expo-asset, not a module id (silent in the APK, fine in Expo Go); onTextLayout is a no-op on web; starting potions resolve to archive consumables with both ids accepted — READ before sheet decks, audio, web text sizing or starting inventory

- [v0.27.2 verified diagnosis](runekeep_v0272_diagnosis.md) — collapsed folders were SAVED but never read back (roster held a duplicate Set); the pop-up frame was a stretched SVG whose inner edge is a FRACTION while the fill was a flat 8dp inset; the gallery FlatList had ZERO windowing; live cards must ride the withImage window (do NOT drop them on native); REST_FRAC eats 64% of any header trim; Android "suspended" = the oboe stream never opened — READ before folder/roster state, pop-up chrome, gallery perf, carousel headroom or audio work

- [v0.27.3 verified diagnosis](runekeep_v0273_diagnosis.md) — the creator lock-up was a Pan gesture REBUILT MID-DRAG latching `grind` (not the forge queue); Android audio was the app double-starting its own oboe stream; the web audio node facade has NO `detune` so every varied sound threw; web needs `overflow: clip`, `hidden` is not enough; a headerless roster group must not hide anything — READ before carousel gestures, audio, web shell CSS or history work

- [Card text + platform metrics](runekeep_card_text_platform.md) — the Katana printing over the footer was NOT long text, it was UNSET lineHeight being ~21dp taller across four stat rows on Android than in Chrome; pin lineHeight on every forged-card text, and lib/fit-text is a PURE size estimator because adjustsFontSizeToFit is a web no-op and layout-time values race the bitmap cache — READ before any card layout or "text overflows on one platform" report

- [.rune extension + web sharing](runekeep_rune_ext_sharing.md) — v0.30.0 writes .rune but the envelope tag stays "rkp" ON PURPOSE so exports open on older installs; exportRkp/pickRkp now work in a browser (Blob download, asset.file.text()); the card-menu share wedge is UNCONDITIONAL and nfcAvailable only picks its label — READ before file-format, export/import or share-entry-point work

- [v0.35 DM + print diagnoses](runekeep_v0350_dm_print.md) - view-shot's captureRef THROWS on web (findNodeHandle unsupported, use html2canvas on ref.current); the PDF's app icons were the un-forged placeholder in `item.source` (use printFaces, never source, when `live` is set); the web print frame is about:blank so relative asset URLs break; DM modifiers are materialised as CARDS on the character file, which is why history/export/import all handle them for free; the stat-radial gesture was rebuilt mid-hold (the v0.27.3 trap again) - READ before print, view-shot, DM-modifier or radial work

- [v0.36-v0.38 Characterize, the wheel, the cards panel, the wheel's real crash, shared files](runekeep_v0360_characterize.md) - the stat wheel died on an animated TRANSFORM around an <Svg> (native Fabric, invisible to every JS guard, four earlier fixes were all real and all insufficient); a shared content:// CANNOT be read with the modern expo-file-system File API (SAFDocumentFile takes fromTreeUri unless pathSegments[0] is literally "document", and that throws from exists(), which size() and text() both call) so copy it with the LEGACY copyAsync first; +native-intent must not apply the image-picker media guard; adjustsFontSizeToFit is a web no-op so card titles are sized by arithmetic into a fixed one-line band; carousel layer windows gate on ROW CENTRE so a long drag empties the card; floor(n/2) opens a two-card carousel on the LAST card; GEAR_SWIPE_PX is the gear sensitivity and smaller is more; DmModal's tap absorber fights a platform ScrollView; Characterize = one pure module + charId on the Combatant + a threshold back-solve - READ before stat-wheel, card-title, card-drag, shared-file, gear-scroll, DM-modal-scroll or characterize work ALSO v0.37: hold-to-drag is DELETED from the cards gallery (fourth gesture rebuilt mid-gesture, the ref guard did not save it) and tap/hold rules live in gallery-select.ts; a modal zIndex loses to a panel two levels ABOVE it in the tree (Share drew under the Cards panel); Move goes through movedFirst so a move into a card own category promotes it; PLAQUE_BLEED=2; the six expansion classes got class plaques from their two domains - READ before gallery gesture, overlay-hidden, card-move or plaque work ALSO v0.37.1: a desktop press released OUTSIDE the phone column never reached the app (RNGH pointer capture; the window-level composedPath shim in +html.tsx, mouse only, and synthetic input cannot reproduce it) and the Scar card TYPE carries its own +1 scar effect; PLAQUE_BLEED is 1 now. ALSO v0.38: the Toggle needs toggleable AND applying (the identity exclusion is gone); sorting a hand is card-sort (pure, CHROMA not HSL saturation) + card-sort-entries + scripts/card_art_colors.py, and the sort animation IS the drop reused - READ before desktop-web pointer, card-type-effect, toggle or card-sorting work

- [v0.39.0 dice tray + overlay placement](runekeep_v0390_dice_overlays.md) - the REACT COMPILER is on and silently memoises a component that renders a mutable ref (no error, live subscription, nothing repaints); an absolute inset-0 overlay inside a ScrollView is placed against the scroll CONTENT, which is the whole off-centre pop-up bug (components/overlay-host.tsx is the fix); a debounce timer that never nulled its handle made every focus flush a dead timer and write a stale encounter over a characterized one; the dice tray is lib/dice-pool + dice-tray.tsx, transform-only, and Rune.sheet is the PARCHMENT not a text colour - READ before overlay/dialog placement, debounced-save, "it will not re-render", dice or card-type-effect work

- [v0.40.0 mirror + detent watchdog](runekeep_v0400_mirror_detent.md) - the REPO IS PUBLIC so assets/temp (licensed rulebook PDFs/scans) must NEVER be pushed and .claude/memory + docs/fresh-machine.md are the handover instead; the sheet's upper band was 5 design px out (measure via a known-size element's getBoundingClientRect, not by eye); the dice button is the armor panel MIRRORED with scaleX(-1) about the diamond axis, never a hand-cut path; a DETENT WATCHDOG that samples until the value is AT REST fixes every stranded-carousel path at once where a gesture branch fixes one; on web a swipe ALSO fires a DOM click so a drag flag must swallow it; rolling twice over one throw is what leaves dice at crooked angles - READ before sheet-geometry, mirrored-art, carousel-snap, web-tap-after-drag, dice-animation or repo-contents work
